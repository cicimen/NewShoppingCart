@*@model IList<ShoppingCart.Domain.Entities.Product>*@
@model ShoppingCart.UI.Models.ProductsListViewModel

@{
    ViewBag.Title = "Home Page";
}

@*<div class="jumbotron">
    <h1>ASP.NET</h1>
    <p class="lead">ASP.NET is a free web framework for building great Web sites and Web applications using HTML, CSS and JavaScript.</p>
    <p><a href="http://asp.net" class="btn btn-primary btn-large">Learn more &raquo;</a></p>
</div>*@

@*@foreach (var item in Model)
{
    <div>
        <b>@item.ProductTranslations.Single(x => x.Language.LanguageCode == "tr").ProductName</b> 
        <b>@item.Category.CategoryTranslations.Single(x => x.Language.LanguageCode == "tr").CategoryName</b>
        
        @foreach (var attribute in item.ProductAttributes)
	    {
            @attribute.ProductAttributeTranslations.Single(x=>x.Language.LanguageCode=="tr").ProductAttributeName
	    }

    </div>
}*@

@if (Model.CurrentCategory != null)
{

    @Html.GetCategoryBreadCrumb(Model.CurrentCategory, x => Url.Action("Index", new { page = 1, categoryURLText = x }))
}


<div class="row mason">
    <div class="list-group">
    @foreach (var p in Model.Products)
    {
        Html.RenderPartial("ProductSummary", p);
    }
    </div>
</div>

<div class="pager">
    @Html.PageLinks(Model.PagingInfo, x => Url.Action("Index", new { page = x, categoryURLText = Model.CurrentCategory }))
</div>

